{% set carousel_config = {
    loop: true,
    speed: 400,
    autoplay: {
        delay: autoplay_dealy|default(2000)
    },
    pagination: false,
} %}
{% set arrows_class = 'd-flex justify-content-between centered-absolute w-100 px-3' %}
{% extends get_template('carousel', _self) %}

