<header class="text-{% if is_center|default(1) %}center{% else %}left{% endif %}">
    {{ render_if_has_content('h2', title_attrs|default({class: 'h2 mb-3'}), title) }}
    {{ render_if_has_content('div', description_attrs|default({class: ''}), wpautop(description)) }}
</header>

